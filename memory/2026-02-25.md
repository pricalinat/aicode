
- 用户偏好（工具降级顺序）：若 Claude Code 因 token 不可用，则切换到 Codex；若 Codex 也不可用，再切换到 OpenCode（记录于 2026-02-25）。

- 用户偏好更新：Claude 默认不用 yolo；仅在权限阻塞时才使用 `--dangerously-skip-permissions`。若 Claude 不可用→Codex；Codex 不可用→OpenCode。（2026-02-25）
- 方向决策（用户新增）：知识图谱必须形成数据闭环。造数是核心能力（基础供给信息 + 用户行为），并持续迭代沉淀为特征；检索同样是核心。后续迭代重点转向“人找供给”和“供给找人”的双向匹配能力建设。（2026-02-25）
- 用户提醒：Claude token 刷新节奏为每天 00:00 首次刷新，之后每 5 小时刷新一次（用于调度/降级策略）。记录于 2026-02-25。
- Claude 后续使用  避免权限确认超时（2026-02-26）
- Claude 后续使用 --dangerously-skip-permissions 避免权限确认超时（2026-02-26）
